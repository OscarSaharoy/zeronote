* {
	margin: 0;
	padding: 0;
}

body, #container {
	width: 100vw;
	height: 100vh;
	overflow: hidden;
	display: grid;
	justify-items: stretch;
	align-items: stretch;
}

svg#canvas {
	background-color: white;	
	touch-action: none;
}

#toolbox {
	display: grid;
	grid-gap: 1rem;
	position: absolute;
	padding: 1rem;
	padding-bottom: 1.6rem;
	left: 1rem;
	top: 1rem;
	background-color: rgba(255,255,255,0.8);
	justify-items: center;
	border-radius: 1rem;
	user-select: none;
}

#toolbox img, #toolbox svg {
	overflow: visible;
}

#toolbox #logo {
	width: 4rem;
	margin-bottom: 0.2rem;
}

#toolbox img.button {
	height: 2rem;
	cursor: pointer;
}
#toolbox .button:hover {
	filter: brightness(70%);
}
#toolbox .button:active, #toolbox img.button.active {
	filter: saturate(1000%);
	transform: scale(1.15);
}

#toolbox #redo {
	transform: scaleX(-1);
}
#toolbox #redo.active, #toolbox #redo:active {
	transform: scaleX(-1) scale(1.15);
}

#toolbox .expand-holder {
	position: relative;
	width: 2rem;
	display: grid;
	grid-template-columns: auto auto;
}
#toolbox .expand-holder input {
	width: 0rem;
	outline: none;
	border: none;
	margin-right: 0.75rem;
	transition: width 0.5s;
}
#toolbox .expand-holder input.expanded {
	width: 10rem;
	display: inline;
}
#toolbox #colour-picker {
	height: 2rem;
	cursor: pointer;
}

#toolbox input[type="file"] {
	display: none;
}

#toolbox #grab {
	margin-top: .5rem;
	width: 1.4rem;
	cursor: grab;
	filter: brightness(70%);
}
